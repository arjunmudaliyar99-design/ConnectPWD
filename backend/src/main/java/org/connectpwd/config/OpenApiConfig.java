package org.connectpwd.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Springdoc OpenAPI configuration — add when springdoc dependency is added
}
